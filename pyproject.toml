[project]
name = "trackfinance"
version = "0.1.0"
description = "Track your finances in a gamified way!"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiosqlite>=0.22.1",
    "fastapi>=0.128.0",
    "fastapi-users[sqlalchemy]>=15.0.3",
    "python-dotenv>=1.2.1",
    "uvicorn[standard]>=0.40.0",
]
